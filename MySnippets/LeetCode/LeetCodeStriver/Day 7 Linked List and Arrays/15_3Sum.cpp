#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
	vector<vector<int> > threeSum(vector<int> &arr) {
		sort(arr.begin(), arr.end());
		for (int i = 0; i < n - 2; i++) { // skipping n-1, n-2
			int new_target = 0 - arr[i];
			//  see from leetcode full solution done by me
		}
	}
};

int main() {

}